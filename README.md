# adobe-stock-auto-metadata
Google chrome extension for adobe stock.

This Chrome Extension can be installed and used locally using **Developer Mode** in Google Chrome.

## 📦 Requirements
- Google Chrome (latest version recommended)
- Extension source code (cloned or downloaded)
- Gemini API Key (Currently)

---

## 🚀 Installation (Developer Mode)

Follow the steps below to install the extension locally:

### 1️⃣ Clone or Download the Repository
Clone this repository or download it as a ZIP file and extract it.

```bash
git clone https://github.com/Ruxxia/adobe-stock-auto-metadata.git
```

### 2️⃣ Go to Google Chrome extension page.
go to google chrome extension page by typing below in the URL.

```bash
chrome://extensions/
```

Enable the developer mode on the top-right corner of the Extensions page

### 3️⃣ Load the Extension

Click Load unpacked

Select the root folder of the extension (the folder that contains manifest.json)

Click Select Folder

✅ The extension should now be installed and visible in Chrome.

## 🧪 How to Use

#### 1️⃣ Add your Gemini API key to the `key.txt` file.
#### 2️⃣ Open the Adobe Stock Contributor upload page and upload your image or video files.
#### 3️⃣ Refresh the page so the extension can detect the required elements.
#### 4️⃣ Select the first image, open the extension, set the generation delay, then click **Generate**.



